/* common.h -- umbrella header included by the splat-generated translation
 * units. splat emits `#include "common.h"` at the top of every `c` subsegment
 * it writes, so this file has to exist even though it currently only pulls in
 * the INCLUDE_ASM machinery. Project-wide types and declarations belong here
 * as the decompilation grows.
 */
#ifndef COMMON_H
#define COMMON_H

#include "include_asm.h"

#endif /* COMMON_H */
